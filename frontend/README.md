# RaceOS Frontend

React and TypeScript dashboard for RaceOS.

## Commands

```bash
npm install
npm start
npm run build
npm test
```

The development server runs at `http://localhost:3000` and expects the FastAPI backend at `http://127.0.0.1:8000`.

See the root `README.md` for full setup, data ingestion, API, and repository structure details.
